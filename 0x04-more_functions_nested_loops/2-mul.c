#include "main.h"

/**
 *  mul - multiplies two integers, a and b
 *  @a: first int
 *  @b: second int
 *  Return: the multiplication of a and b
 */
int mul(int a, int b);
{
	int a;
	int b;

	if (a >= '0' && c <= '0')

		return (a * b);
}
