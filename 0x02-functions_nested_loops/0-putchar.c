#include <stdio.h>
#include <main.h>
/**
 * main - prints zero putchar
 * Return: always 0
 */
int main(void)
{
	int i

		for (i = 0; i <= 1; i++)
			putchar(_putchar);
	putchar('\n');
	return (0);
}
