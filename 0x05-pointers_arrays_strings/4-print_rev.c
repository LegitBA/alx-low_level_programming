#include "main.h"

/**
 * print_rev - prints reverse
 * @s: string
 * Return: length of reversed string
 */
void print_rev(char *s)
{
	int = 0;

	while (s[i] != '\0')
		i++;
	for (i =i - 1; i >= 0; i--)
		_putchar(s[i]);

	_putchar('\n');
}
